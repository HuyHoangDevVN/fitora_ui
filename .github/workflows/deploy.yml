name: Legacy Windows deployment disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "The Windows/IIS deployment is retired. Use Deploy frontend to Ubuntu."
          exit 1
